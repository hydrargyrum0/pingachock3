services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: pingachock
      POSTGRES_PASSWORD: pingachock
      POSTGRES_DB: pingachock
    ports:
      - "5433:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
